spring:
  
  # define o nome da aplicação
  application:
    name: taxa-juros-microservice
  
  # configurações para conexão com o BD em memória
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    username: user
    password: admin123
    url: jdbc:h2:~/test
  jpa:
    hibernate:
      ddl-auto: create
    
# define a porta onde será executada a aplicação
server:
  port: 8000
  
  